#include <turnkey/base>
#include <turnkey/mysql>

build-essential # needed by node-gyp for packages with native bindings (e.g., node-inspector)
ca-certificates
openssl

nginx

python3-bcrypt
python3-pip

sudo
